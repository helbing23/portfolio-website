export interface Education {
    id: string;
    title: string;
    degree: string;
    university: string;
    institution: string;
    startDate: string,
    endDate: string;
    location: string;
    link?: string;
    favicon?: string;
}