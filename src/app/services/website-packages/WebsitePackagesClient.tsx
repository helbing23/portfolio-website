"use client";

import Link from "next/link";
import Container from "@/components/ui/Container";
import Reveal from "@/components/ui/Reveal";
import { FaCheck } from "react-icons/fa";

interface Package {
  name: string;
  price: string;
  priceNote?: string;
  summary: string;
  features: string[];
  footnote?: string;
  highlighted?: boolean;
}

const packages: Package[] = [
  {
    name: "Flexible Page Development",
    price: "£100",
    priceNote: "per page, minimum 3 pages",
    summary:
      "Build or expand your site one page at a time. Projects start at £300.",
    features: [
      "Custom webpage creation",
      "Page layout and visual structure",
      "Image sourcing and integration",
      "Mobile and tablet responsiveness",
      "Your content integrated and formatted",
    ],
    footnote: "Final cost depends on page complexity.",
  },
  {
    name: "Basic Website Package",
    price: "£1,000",
    priceNote: "up to 6 pages",
    summary:
      "A complete website for individuals and small businesses. Built from the ground up, not from a template.",
    features: [
      "Custom page design and layout",
      "Image sourcing and integration",
      "Mobile and tablet responsiveness",
      "Contact form integration",
      "Basic SEO setup",
      "Ready for launch",
    ],
    footnote: "Typical pages are Home, About, Services, Portfolio, Blog and Contact.",
    highlighted: true,
  },
  {
    name: "Premium Website Package",
    price: "£2,000",
    priceNote: "starting from, custom scoped",
    summary:
      "For organisations that need more structure, stronger visual identity and deeper planning.",
    features: [
      "Expanded website architecture",
      "Advanced page layouts and design",
      "Strategic content structuring",
      "Additional integrations and functionality",
      "SEO-ready site structure",
      "Production-grade delivery",
    ],
    footnote: "Scoped and quoted per project.",
  },
];

const clientProvides = [
  "Written text for each page",
  "Business information and service descriptions",
  "Your logo",
  "Brand colours, if you have them",
  "Images relevant to your business",
];

const addOns = [
  "Domain registration",
  "Hosting setup",
  "Website maintenance",
  "Content development",
  "SEO optimisation",
  "Analytics and performance tracking",
  "Website text writing",
  "Logo creation",
  "Brand colour scheme development",
];

export default function WebsitePackagesClient() {
  return (
    <section className="py-16 md:py-24">
      <Container as="div">
        <header className="mb-12">
          <h1 className="text-4xl font-bold mb-4">Website packages</h1>
          <p className="mb-4">
            Fixed-price websites for consultants, startups and small businesses
            that need a credible online presence. Clear scope, clear price, no
            surprises.
          </p>
          <p className="text-muted-foreground">
            A website should do more than exist. It should make people trust you
            before you have said a word. Every site here is built to production
            standard and ready for launch, and I stay available afterwards for
            fixes, changes and new pages.
          </p>
        </header>

        <Reveal>
          <div className="mb-12 grid gap-6 md:grid-cols-3">
            {packages.map((pkg) => (
              <div
                key={pkg.name}
                className={`flex flex-col rounded-xl border bg-white/70 p-6 backdrop-blur-sm transition duration-200 hover:shadow-xl dark:bg-black ${
                  pkg.highlighted
                    ? "border-green-600/50 shadow-xl"
                    : "border-gray-200/50 dark:border-white/[0.2] dark:shadow-none"
                }`}
              >
                <h2 className="text-lg font-bold gradient-text">{pkg.name}</h2>
                <p className="mt-3">
                  <span className="text-3xl font-bold">{pkg.price}</span>
                  {pkg.priceNote && (
                    <span className="ml-2 text-sm text-muted-foreground">
                      {pkg.priceNote}
                    </span>
                  )}
                </p>
                <p className="mt-3 text-sm text-neutral-600 dark:text-neutral-300">
                  {pkg.summary}
                </p>
                <ul className="mt-4 flex-grow space-y-2">
                  {pkg.features.map((feature) => (
                    <li key={feature} className="flex items-start gap-2 text-sm">
                      <FaCheck
                        className="mt-1 shrink-0 text-green-600"
                        size={12}
                        aria-hidden="true"
                      />
                      {feature}
                    </li>
                  ))}
                </ul>
                {pkg.footnote && (
                  <p className="mt-4 text-xs text-muted-foreground">
                    {pkg.footnote}
                  </p>
                )}
                <Link
                  href="/booking"
                  className={`mt-6 rounded-xl px-4 py-2.5 text-center text-sm font-medium transition-all hover:scale-105 ${
                    pkg.highlighted
                      ? "bg-green-600 text-white shadow-lg hover:bg-green-700"
                      : "border border-green-600 text-green-600 hover:bg-green-600 hover:text-white dark:text-green-500 dark:hover:text-white"
                  }`}
                >
                  Discuss this package
                </Link>
              </div>
            ))}
          </div>
        </Reveal>

        <div className="mb-12 grid gap-6 md:grid-cols-2">
          <Reveal className="rounded-xl border border-gray-200/50 bg-white/70 p-6 backdrop-blur-sm dark:border-white/[0.2] dark:bg-black dark:shadow-none">
            <h2 className="text-xl font-semibold mb-3">What I need from you</h2>
            <p className="mb-4 text-sm text-neutral-600 dark:text-neutral-300">
              The packages assume you provide your own content. That keeps the
              build fast and the site accurate to your business.
            </p>
            <ul className="space-y-2">
              {clientProvides.map((item) => (
                <li key={item} className="flex items-start gap-2 text-sm">
                  <FaCheck
                    className="mt-1 shrink-0 text-green-600"
                    size={12}
                    aria-hidden="true"
                  />
                  {item}
                </li>
              ))}
            </ul>
            <p className="mt-4 text-sm text-muted-foreground">
              If you would rather I write the website text for you, that can be
              arranged as an extra and quoted separately.
            </p>
          </Reveal>

          <Reveal delay={0.1} className="rounded-xl border border-gray-200/50 bg-white/70 p-6 backdrop-blur-sm dark:border-white/[0.2] dark:bg-black dark:shadow-none">
            <h2 className="text-xl font-semibold mb-3">Optional add-ons</h2>
            <p className="mb-4 text-sm text-neutral-600 dark:text-neutral-300">
              Quoted separately depending on scope.
            </p>
            <ul className="grid grid-cols-1 gap-2 sm:grid-cols-2">
              {addOns.map((item) => (
                <li key={item} className="flex items-start gap-2 text-sm">
                  <FaCheck
                    className="mt-1 shrink-0 text-green-600"
                    size={12}
                    aria-hidden="true"
                  />
                  {item}
                </li>
              ))}
            </ul>
          </Reveal>
        </div>

        <Reveal className="mb-12 rounded-xl border border-gray-200/50 bg-white/70 p-6 backdrop-blur-sm dark:border-white/[0.2] dark:bg-black dark:shadow-none">
          <h2 className="text-xl font-semibold mb-3">Timeline and terms</h2>
          <p className="text-sm text-neutral-600 dark:text-neutral-300 mb-2">
            Most sites are done in 7 to 14 working days, depending on size and
            scope. Simple sites are quicker. 50% of the fee is due before work
            starts, the rest on completion.
          </p>
        </Reveal>

        <Reveal>
          <div className="rounded-xl border border-gray-200/50 bg-white/70 p-8 text-center backdrop-blur-sm dark:border-white/[0.2] dark:bg-black dark:shadow-none">
            <h2 className="text-2xl font-semibold mb-2">
              Tell me about your website
            </h2>
            <p className="text-muted-foreground mb-6 max-w-xl mx-auto">
              Thirty minutes on a call is enough to work out which package fits
              and what it will cost. No obligation on either side.
            </p>
            <Link
              href="/booking"
              className="inline-block rounded-xl bg-green-600 px-6 py-3 text-sm font-medium text-white shadow-lg transition-all hover:scale-105 hover:bg-green-700"
            >
              Book an intro call
            </Link>
          </div>
        </Reveal>
      </Container>
    </section>
  );
}
